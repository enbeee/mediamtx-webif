**This is vibe-coded and probably riddled with bugs. Don't @ me!**

Add your API-enabled instance by hitting the plus in the top left corner. Connection info/credentials are only saved in local storage in your web browser.

Also remember that changes made through the API won't be saved to the YAML caonfig of your instance, so use the import/export feature on the top right of the web interface.
